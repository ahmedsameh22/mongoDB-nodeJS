{{>header}}
{{>navbar}}
<div class="container">
    {{#if hasData}}
    <table class="table ">
     <thead>
        <tr>
            <th>#</th>
            <th>name</th>
            <th>email</th>
            <th>userNAme</th>
        </tr>
     </thead>
     <tbody>
        <tr>
            <td>{{data.name}}</td>
            <td>{{data.age}}</td>
           <td>{{data.job}}</td>
           <td>{{data.userType}}</td>

        </tr>
     </tbody>
    </table>
    {{else}}
    <div class=" alret alert-danger">not user have this ID</div>
    {{/if}}
</div>
{{>footer}}
